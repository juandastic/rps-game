

const initialDataState = {
  '1': {
    user1: 'JUAN123',
    user2: 'JUAN123',
    winner: '',
    roundsNumber: '3',
    rounds:[]
  }
}

export default (state = initialDataState, action) => {
  switch (action.type) {
    default:
      return state
  }
}